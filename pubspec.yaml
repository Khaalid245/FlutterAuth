name: authinaction
description: "A new Flutter project."

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.24.2
  firebase_auth: ^4.17.4
  cupertino_icons: ^1.0.8
  fluentui_system_icons: ^1.1.273
  flutter_svg: ^2.0.17
  get: ^4.6.5  # Add GetX here!

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png
    - assets/file.svg
